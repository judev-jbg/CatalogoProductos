<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.CatalogoProductos" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->

    </style>

    <style name="Theme.App.SplashScreen" parent="Theme.CatalogoProductos">
        <!-- Fondo blanco del splash screen -->
        <item name="windowSplashScreenBackground">@android:color/white</item>



        <!-- Icono del splash screen (opcional) -->
        <!--<item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item> -->

        <!-- Color del icono (opcional) -->
        <!--<item name="windowSplashScreenIconBackgroundColor">@color/md_theme_light_primary</item> -->

        <!-- Duración de la animación (opcional) -->
        <!--<item name="windowSplashScreenAnimationDuration">500</item> -->

        <!-- Tema que se aplica después del splash -->
        <item name="postSplashScreenTheme">@style/Theme.CatalogoProductos</item>
    </style>

</resources>